# Исключения статического чека анимаций (scripts/check-animations.mjs).
# Формат: `свойство — причина`. Пусто — и хорошо: анимируем только
# transform/opacity/цвета, layout-свойства дёргают страницу.
